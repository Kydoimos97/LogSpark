# SparkLogger

::: logspark.SparkLoggerDef.SparkLogger

## Configuration Notes

**Default Handler**: If both handler and preset are None, TerminalHandler is used.

**Kill Warning**: Not safe in production; references to old stdlib logger may persist.

## Global Instance

::: logspark.spark_logger